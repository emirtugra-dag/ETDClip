<Window x:Class="ETDClip.MainWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        mc:Ignorable="d"
        Title="ETDClip" Height="680" Width="490"
        WindowStyle="None" AllowsTransparency="True" Background="Transparent"
        WindowStartupLocation="CenterScreen"
        ShowInTaskbar="False"
        SourceInitialized="Window_SourceInitialized"
        Loaded="Window_Loaded" Closing="Window_Closing">

    <Window.Resources>
        <SolidColorBrush x:Key="BgBrush" Color="#0F172A"/>
        <SolidColorBrush x:Key="SurfaceBrush" Color="#1E293B"/>
        <SolidColorBrush x:Key="SurfaceHoverBrush" Color="#283852"/>
        <SolidColorBrush x:Key="BorderBrush" Color="#334755"/>
        <SolidColorBrush x:Key="TextPrimaryBrush" Color="#F8FAFC"/>
        <SolidColorBrush x:Key="TextSecondaryBrush" Color="#94A3B8"/>
        <SolidColorBrush x:Key="AccentBrush" Color="#3B82F6"/>
        <SolidColorBrush x:Key="AccentHoverBrush" Color="#60A5FA"/>
        <SolidColorBrush x:Key="ModalOverlayBrush" Color="#D0090E17"/>

        <!-- Thin scrollbar -->
        <Style TargetType="{x:Type ScrollBar}">
            <Setter Property="Stylus.IsPressAndHoldEnabled" Value="false"/>
            <Setter Property="Width" Value="5"/>
            <Setter Property="Margin" Value="2,0,2,0"/>
            <Setter Property="Template">
                <Setter.Value>
                    <ControlTemplate TargetType="{x:Type ScrollBar}">
                        <Grid>
                            <Track x:Name="PART_Track" IsDirectionReversed="true">
                                <Track.DecreaseRepeatButton>
                                    <RepeatButton Command="{x:Static ScrollBar.LineUpCommand}" Visibility="Collapsed"/>
                                </Track.DecreaseRepeatButton>
                                <Track.Thumb>
                                    <Thumb>
                                        <Thumb.Template>
                                            <ControlTemplate TargetType="{x:Type Thumb}">
                                                <Border CornerRadius="3" Background="{DynamicResource BorderBrush}" Opacity="0.7"/>
                                                <ControlTemplate.Triggers>
                                                    <Trigger Property="IsMouseOver" Value="True">
                                                        <Setter Property="Background" Value="{DynamicResource AccentBrush}"/>
                                                        <Setter Property="Opacity" Value="1"/>
                                                    </Trigger>
                                                </ControlTemplate.Triggers>
                                            </ControlTemplate>
                                        </Thumb.Template>
                                    </Thumb>
                                </Track.Thumb>
                                <Track.IncreaseRepeatButton>
                                    <RepeatButton Command="{x:Static ScrollBar.LineDownCommand}" Visibility="Collapsed"/>
                                </Track.IncreaseRepeatButton>
                            </Track>
                        </Grid>
                    </ControlTemplate>
                </Setter.Value>
            </Setter>
        </Style>

        <!-- Category tab -->
        <Style x:Key="CategoryTabStyle" TargetType="RadioButton">
            <Setter Property="Background" Value="{DynamicResource SurfaceBrush}"/>
            <Setter Property="Foreground" Value="{DynamicResource TextSecondaryBrush}"/>
            <Setter Property="Padding" Value="12,6"/>
            <Setter Property="Margin" Value="0,0,6,0"/>
            <Setter Property="BorderThickness" Value="1"/>
            <Setter Property="BorderBrush" Value="{DynamicResource BorderBrush}"/>
            <Setter Property="Cursor" Value="Hand"/>
            <Setter Property="FontSize" Value="12"/>
            <Setter Property="Template">
                <Setter.Value>
                    <ControlTemplate TargetType="RadioButton">
                        <Border x:Name="border"
                                Background="{TemplateBinding Background}"
                                BorderBrush="{TemplateBinding BorderBrush}"
                                BorderThickness="{TemplateBinding BorderThickness}"
                                CornerRadius="8" Padding="{TemplateBinding Padding}">
                            <ContentPresenter HorizontalAlignment="Center" VerticalAlignment="Center"/>
                        </Border>
                        <ControlTemplate.Triggers>
                            <Trigger Property="IsChecked" Value="True">
                                <Setter TargetName="border" Property="Background" Value="{DynamicResource AccentBrush}"/>
                                <Setter TargetName="border" Property="BorderBrush" Value="{DynamicResource AccentHoverBrush}"/>
                                <Setter Property="Foreground" Value="#FFFFFF"/>
                            </Trigger>
                            <Trigger Property="IsMouseOver" Value="True">
                                <Setter TargetName="border" Property="BorderBrush" Value="{DynamicResource AccentBrush}"/>
                            </Trigger>
                        </ControlTemplate.Triggers>
                    </ControlTemplate>
                </Setter.Value>
            </Setter>
        </Style>

        <!-- Action button -->
        <Style x:Key="GlassButtonStyle" TargetType="Button">
            <Setter Property="Background" Value="{DynamicResource SurfaceBrush}"/>
            <Setter Property="Foreground" Value="{DynamicResource TextPrimaryBrush}"/>
            <Setter Property="BorderBrush" Value="{DynamicResource BorderBrush}"/>
            <Setter Property="BorderThickness" Value="1"/>
            <Setter Property="Padding" Value="10,6"/>
            <Setter Property="Cursor" Value="Hand"/>
            <Setter Property="FontSize" Value="13"/>
            <Setter Property="Template">
                <Setter.Value>
                    <ControlTemplate TargetType="Button">
                        <Border x:Name="border"
                                Background="{TemplateBinding Background}"
                                BorderBrush="{TemplateBinding BorderBrush}"
                                BorderThickness="{TemplateBinding BorderThickness}"
                                CornerRadius="8" Padding="{TemplateBinding Padding}">
                            <ContentPresenter HorizontalAlignment="Center" VerticalAlignment="Center"/>
                        </Border>
                        <ControlTemplate.Triggers>
                            <Trigger Property="IsMouseOver" Value="True">
                                <Setter TargetName="border" Property="Background" Value="{DynamicResource SurfaceHoverBrush}"/>
                                <Setter TargetName="border" Property="BorderBrush" Value="{DynamicResource AccentHoverBrush}"/>
                            </Trigger>
                            <Trigger Property="IsPressed" Value="True">
                                <Setter TargetName="border" Property="Background" Value="{DynamicResource AccentBrush}"/>
                            </Trigger>
                        </ControlTemplate.Triggers>
                    </ControlTemplate>
                </Setter.Value>
            </Setter>
        </Style>

        <!-- TextBox -->
        <Style x:Key="SettingsTextBoxStyle" TargetType="TextBox">
            <Setter Property="Background" Value="{DynamicResource SurfaceBrush}"/>
            <Setter Property="Foreground" Value="{DynamicResource TextPrimaryBrush}"/>
            <Setter Property="BorderBrush" Value="{DynamicResource BorderBrush}"/>
            <Setter Property="BorderThickness" Value="1"/>
            <Setter Property="Padding" Value="10,8"/>
            <Setter Property="FontSize" Value="12"/>
            <Setter Property="CaretBrush" Value="{DynamicResource AccentHoverBrush}"/>
            <Setter Property="SelectionBrush" Value="{DynamicResource AccentBrush}"/>
            <Setter Property="Template">
                <Setter.Value>
                    <ControlTemplate TargetType="TextBox">
                        <Border x:Name="border"
                                Background="{TemplateBinding Background}"
                                BorderBrush="{TemplateBinding BorderBrush}"
                                BorderThickness="{TemplateBinding BorderThickness}"
                                CornerRadius="8">
                            <ScrollViewer x:Name="PART_ContentHost"
                                          Padding="{TemplateBinding Padding}"
                                          VerticalAlignment="Center"/>
                        </Border>
                        <ControlTemplate.Triggers>
                            <Trigger Property="IsFocused" Value="True">
                                <Setter TargetName="border" Property="BorderBrush" Value="{DynamicResource AccentBrush}"/>
                                <Setter TargetName="border" Property="BorderThickness" Value="1.5"/>
                            </Trigger>
                            <Trigger Property="IsMouseOver" Value="True">
                                <Setter TargetName="border" Property="BorderBrush" Value="{DynamicResource BorderBrush}"/>
                            </Trigger>
                        </ControlTemplate.Triggers>
                    </ControlTemplate>
                </Setter.Value>
            </Setter>
        </Style>

        <!-- ComboBoxItem (dropdown row) -->
        <Style x:Key="DarkComboBoxItemStyle" TargetType="ComboBoxItem">
            <Setter Property="Background" Value="{DynamicResource SurfaceBrush}"/>
            <Setter Property="Foreground" Value="{DynamicResource TextPrimaryBrush}"/>
            <Setter Property="Padding" Value="10,8"/>
            <Setter Property="FontSize" Value="12"/>
            <Setter Property="Template">
                <Setter.Value>
                    <ControlTemplate TargetType="ComboBoxItem">
                        <Border x:Name="bg"
                                Background="{TemplateBinding Background}"
                                Padding="{TemplateBinding Padding}">
                            <ContentPresenter/>
                        </Border>
                        <ControlTemplate.Triggers>
                            <Trigger Property="IsMouseOver" Value="True">
                                <Setter TargetName="bg" Property="Background" Value="{DynamicResource SurfaceHoverBrush}"/>
                            </Trigger>
                            <Trigger Property="IsSelected" Value="True">
                                <Setter TargetName="bg" Property="Background" Value="{DynamicResource AccentBrush}"/>
                            </Trigger>
                        </ControlTemplate.Triggers>
                    </ControlTemplate>
                </Setter.Value>
            </Setter>
        </Style>

        <!-- ComboBox (full control with dark dropdown) -->
        <Style x:Key="SettingsComboBoxStyle" TargetType="ComboBox">
            <Setter Property="Background" Value="{DynamicResource SurfaceBrush}"/>
            <Setter Property="Foreground" Value="{DynamicResource TextPrimaryBrush}"/>
            <Setter Property="BorderBrush" Value="{DynamicResource BorderBrush}"/>
            <Setter Property="BorderThickness" Value="1"/>
            <Setter Property="Padding" Value="10,8"/>
            <Setter Property="FontSize" Value="12"/>
            <Setter Property="Cursor" Value="Hand"/>
            <Setter Property="ItemContainerStyle" Value="{StaticResource DarkComboBoxItemStyle}"/>
            <Setter Property="Template">
                <Setter.Value>
                    <ControlTemplate TargetType="ComboBox">
                        <Grid>
                            <!-- ToggleButton that behaves as the background/click area -->
                            <ToggleButton x:Name="ToggleButton"
                                          Background="{TemplateBinding Background}"
                                          BorderBrush="{TemplateBinding BorderBrush}"
                                          BorderThickness="{TemplateBinding BorderThickness}"
                                          Focusable="False"
                                          IsChecked="{Binding IsDropDownOpen, Mode=TwoWay, RelativeSource={RelativeSource TemplatedParent}}"
                                          ClickMode="Press">
                                <ToggleButton.Template>
                                    <ControlTemplate TargetType="ToggleButton">
                                        <Border x:Name="border"
                                                Background="{TemplateBinding Background}"
                                                BorderBrush="{TemplateBinding BorderBrush}"
                                                BorderThickness="{TemplateBinding BorderThickness}"
                                                CornerRadius="8">
                                            <Grid Margin="10,8">
                                                <TextBlock Text="&#x25BE;" Foreground="{DynamicResource TextSecondaryBrush}"
                                                           HorizontalAlignment="Right" VerticalAlignment="Center"
                                                           FontSize="14"/>
                                            </Grid>
                                        </Border>
                                        <ControlTemplate.Triggers>
                                            <Trigger Property="IsMouseOver" Value="True">
                                                <Setter TargetName="border" Property="BorderBrush" Value="{DynamicResource AccentHoverBrush}"/>
                                            </Trigger>
                                            <Trigger Property="IsChecked" Value="True">
                                                <Setter TargetName="border" Property="BorderBrush" Value="{DynamicResource AccentBrush}"/>
                                            </Trigger>
                                        </ControlTemplate.Triggers>
                                    </ControlTemplate>
                                </ToggleButton.Template>
                            </ToggleButton>

                            <!-- Content presenter to show the selected item -->
                            <ContentPresenter x:Name="ContentSite"
                                              IsHitTestVisible="False"
                                              Content="{TemplateBinding SelectionBoxItem}"
                                              ContentTemplate="{TemplateBinding SelectionBoxItemTemplate}"
                                              HorizontalAlignment="Left"
                                              VerticalAlignment="Center"
                                              Margin="12,0,32,0"/>

                            <Popup x:Name="PART_Popup"
                                   Placement="Bottom" IsOpen="{TemplateBinding IsDropDownOpen}"
                                   AllowsTransparency="True" Focusable="False"
                                   PopupAnimation="Slide">
                                <Border Background="{DynamicResource SurfaceBrush}"
                                        BorderBrush="{DynamicResource BorderBrush}" BorderThickness="1"
                                        CornerRadius="8" MinWidth="{TemplateBinding ActualWidth}">
                                    <Border.Effect>
                                        <DropShadowEffect Color="#000" BlurRadius="12" ShadowDepth="4" Opacity="0.4"/>
                                    </Border.Effect>
                                    <ScrollViewer MaxHeight="200">
                                        <StackPanel IsItemsHost="True"/>
                                    </ScrollViewer>
                                </Border>
                            </Popup>
                        </Grid>
                    </ControlTemplate>
                </Setter.Value>
            </Setter>
        </Style>

        <!-- CheckBox -->
        <Style x:Key="SettingsCheckBoxStyle" TargetType="CheckBox">
            <Setter Property="Foreground" Value="{DynamicResource TextPrimaryBrush}"/>
            <Setter Property="FontSize" Value="12"/>
            <Setter Property="Cursor" Value="Hand"/>
            <Setter Property="Margin" Value="0,0,0,8"/>
        </Style>

    </Window.Resources>

    <Border x:Name="MainContainer"
            Background="{DynamicResource BgBrush}" BorderBrush="{DynamicResource BorderBrush}" BorderThickness="1.5"
            CornerRadius="16" Padding="16"
            MouseLeftButtonDown="Window_MouseLeftButtonDown">
        <Border.Effect>
            <DropShadowEffect Color="#000000" BlurRadius="24" ShadowDepth="8" Opacity="0.45"/>
        </Border.Effect>

        <Grid>
            <Grid.RowDefinitions>
                <RowDefinition Height="Auto"/>
                <RowDefinition Height="Auto"/>
                <RowDefinition Height="*"/>
                <RowDefinition Height="Auto"/>
            </Grid.RowDefinitions>

            <!-- ─── HEADER ─── -->
            <Grid Grid.Row="0" Margin="0,0,0,14" MouseLeftButtonDown="Header_MouseLeftButtonDown">
                <Grid.ColumnDefinitions>
                    <ColumnDefinition Width="Auto"/>
                    <ColumnDefinition Width="*"/>
                    <ColumnDefinition Width="Auto"/>
                </Grid.ColumnDefinitions>

                <StackPanel Grid.Column="0" Orientation="Horizontal" VerticalAlignment="Center">
                    <Border CornerRadius="10" Width="38" Height="38" Margin="0,0,10,0" ClipToBounds="True">
                        <Image x:Name="ImgHeaderLogo" Stretch="Uniform"/>
                    </Border>
                    <StackPanel VerticalAlignment="Center">
                        <TextBlock Text="ETDClip" FontSize="17" FontWeight="Bold" Foreground="{DynamicResource TextPrimaryBrush}"/>
                        <TextBlock x:Name="TxtAppSubtitle" Text="Pano Yöneticisi"
                                   FontSize="11" Foreground="{DynamicResource TextSecondaryBrush}"/>
                    </StackPanel>
                </StackPanel>

                <!-- Header buttons — no emojis, clean icons only -->
                <StackPanel Grid.Column="2" Orientation="Horizontal" VerticalAlignment="Center">
                    <Button x:Name="BtnClearAll" Style="{StaticResource GlassButtonStyle}"
                            Click="BtnClearAll_Click" Margin="0,0,6,0" ToolTip="Geçmişi Temizle"
                            Padding="10,6">
                        <StackPanel Orientation="Horizontal" VerticalAlignment="Center">
                            <!-- Trash icon via Unicode -->
                            <TextBlock Text="&#xE107;" FontFamily="Segoe MDL2 Assets"
                                       FontSize="13" VerticalAlignment="Center" Margin="0,0,5,0"/>
                            <TextBlock x:Name="TxtClearAllLabel" Text="Temizle"
                                       FontSize="11" VerticalAlignment="Center"/>
                        </StackPanel>
                    </Button>
                    <Button x:Name="BtnSettings" Style="{StaticResource GlassButtonStyle}"
                            Click="BtnSettings_Click" Margin="0,0,6,0" ToolTip="Ayarlar" Padding="9,6">
                        <!-- Settings icon -->
                        <TextBlock Text="&#xE713;" FontFamily="Segoe MDL2 Assets" FontSize="14"/>
                    </Button>
                    <Button x:Name="BtnClose" Style="{StaticResource GlassButtonStyle}"
                            Click="BtnClose_Click" ToolTip="Gizle" Padding="9,6">
                        <!-- Hide/close icon -->
                        <TextBlock Text="&#xE8BB;" FontFamily="Segoe MDL2 Assets" FontSize="13"/>
                    </Button>
                </StackPanel>
            </Grid>

            <!-- ─── SEARCH + TABS ─── -->
            <StackPanel Grid.Row="1" Margin="0,0,0,14">
                <Border x:Name="SearchBoxBorder" CornerRadius="10" Background="{DynamicResource SurfaceBrush}"
                        BorderBrush="{DynamicResource BorderBrush}" BorderThickness="1" Padding="10,6" Margin="0,0,0,10">
                    <Grid>
                        <TextBlock x:Name="TxtSearchPlaceholder" Text="Pano geçmişinde ara..."
                                   Foreground="{DynamicResource TextSecondaryBrush}" VerticalAlignment="Center"
                                   IsHitTestVisible="False" Margin="4,0,0,0" FontSize="12"/>
                        <TextBox x:Name="TxtSearch" Background="Transparent" BorderThickness="0"
                                 Foreground="{DynamicResource TextPrimaryBrush}" FontSize="13" CaretBrush="{DynamicResource AccentHoverBrush}"
                                 TextChanged="TxtSearch_TextChanged" VerticalAlignment="Center"/>
                        <Button x:Name="BtnClearSearch" Foreground="{DynamicResource TextSecondaryBrush}"
                                Background="Transparent" BorderThickness="0"
                                HorizontalAlignment="Right" Click="BtnClearSearch_Click"
                                Visibility="Collapsed" Cursor="Hand" Padding="4,2">
                            <TextBlock Text="&#xE10A;" FontFamily="Segoe MDL2 Assets" FontSize="10"/>
                        </Button>
                    </Grid>
                </Border>

                <ScrollViewer HorizontalScrollBarVisibility="Auto" VerticalScrollBarVisibility="Disabled">
                    <StackPanel Orientation="Horizontal">
                        <RadioButton x:Name="TabAll"    Content="Tümü"       Tag="All"    IsChecked="True" Style="{StaticResource CategoryTabStyle}" Checked="CategoryTab_Checked"/>
                        <RadioButton x:Name="TabText"   Content="Metin"      Tag="Text"               Style="{StaticResource CategoryTabStyle}" Checked="CategoryTab_Checked"/>
                        <RadioButton x:Name="TabImage"  Content="Resim"      Tag="Image"              Style="{StaticResource CategoryTabStyle}" Checked="CategoryTab_Checked"/>
                        <RadioButton x:Name="TabFile"   Content="Dosya"      Tag="File"               Style="{StaticResource CategoryTabStyle}" Checked="CategoryTab_Checked"/>
                        <RadioButton x:Name="TabPinned" Content="Sabitlendi" Tag="Pinned"             Style="{StaticResource CategoryTabStyle}" Checked="CategoryTab_Checked"/>
                    </StackPanel>
                </ScrollViewer>
            </StackPanel>

            <!-- ─── HISTORY LIST ─── -->
            <ScrollViewer Grid.Row="2" VerticalScrollBarVisibility="Auto" HorizontalScrollBarVisibility="Disabled">
                <StackPanel x:Name="PnlHistoryList" Margin="0,0,4,0"/>
            </ScrollViewer>

            <!-- ─── FOOTER ─── -->
            <Border Grid.Row="3" Margin="0,10,0,0" Padding="0,6,0,0"
                    BorderBrush="{DynamicResource BorderBrush}" BorderThickness="0,1,0,0">
                <Grid>
                    <TextBlock x:Name="TxtStatus" Text="Son 10 öğe gösteriliyor"
                               FontSize="11" Foreground="{DynamicResource TextSecondaryBrush}" VerticalAlignment="Center"
                               TextTrimming="CharacterEllipsis" Margin="0,0,80,0"/>
                    <Border HorizontalAlignment="Right" CornerRadius="6"
                            Background="{DynamicResource SurfaceBrush}" BorderBrush="{DynamicResource BorderBrush}" BorderThickness="1" Padding="6,2">
                        <TextBlock x:Name="TxtHotkeyBadge" Text="Alt+V"
                                   FontSize="10" FontWeight="Bold" Foreground="{DynamicResource AccentHoverBrush}"/>
                    </Border>
                </Grid>
            </Border>

            <!-- ─── SETTINGS MODAL ─── -->
            <Grid x:Name="ModalSettings" Grid.RowSpan="4"
                  Visibility="Collapsed" Background="{DynamicResource ModalOverlayBrush}"
                  MouseDown="ModalSettings_BackgroundClick">
                <Border x:Name="ModalSettingsContent"
                        Width="420" MaxHeight="580"
                        HorizontalAlignment="Center" VerticalAlignment="Center"
                        Background="{DynamicResource BgBrush}" BorderBrush="{DynamicResource BorderBrush}" BorderThickness="1.5"
                        CornerRadius="16" Padding="22">
                    <Border.Effect>
                        <DropShadowEffect Color="#000000" BlurRadius="20" ShadowDepth="6" Opacity="0.5"/>
                    </Border.Effect>
                    <ScrollViewer VerticalScrollBarVisibility="Auto">
                        <StackPanel>
                            <!-- Title -->
                            <Grid Margin="0,0,0,18">
                                <StackPanel Orientation="Horizontal" VerticalAlignment="Center">
                                    <TextBlock Text="&#xE713;" FontFamily="Segoe MDL2 Assets"
                                               FontSize="16" Foreground="#60A5FA"
                                               VerticalAlignment="Center" Margin="0,0,8,0"/>
                                    <TextBlock x:Name="TxtSettingsTitle" Text="ETDClip Ayarlari"
                                               FontSize="16" FontWeight="Bold" Foreground="#F8FAFC"
                                               VerticalAlignment="Center"/>
                                </StackPanel>
                                <Button Foreground="#94A3B8" Background="Transparent"
                                        BorderThickness="0" HorizontalAlignment="Right"
                                        Click="BtnCloseSettings_Click" FontSize="13" Cursor="Hand"
                                        Padding="6,4">
                                    <TextBlock Text="&#xE8BB;" FontFamily="Segoe MDL2 Assets" FontSize="13"/>
                                </Button>
                            </Grid>

                            <!-- Language -->
                            <TextBlock x:Name="LblLanguage" Text="Uygulama Dili / Language"
                                       Foreground="#94A3B8" FontSize="11" FontWeight="SemiBold"
                                       Margin="0,0,0,6"/>
                            <ComboBox x:Name="CmbLanguage" Style="{StaticResource SettingsComboBoxStyle}" Margin="0,0,0,16">
                                <ComboBoxItem Content="Türkçe" Tag="TR" IsSelected="True"/>
                                <ComboBoxItem Content="English" Tag="EN"/>
                            </ComboBox>

                            <!-- Hotkey -->
                            <TextBlock x:Name="LblGlobalHotkey" Text="Global Kısayol Tuşu"
                                       Foreground="#94A3B8" FontSize="11" FontWeight="SemiBold"
                                       Margin="0,0,0,6"/>
                            <Grid Margin="0,0,0,16">
                                <Grid.ColumnDefinitions>
                                    <ColumnDefinition Width="*"/>
                                    <ColumnDefinition Width="Auto"/>
                                </Grid.ColumnDefinitions>
                                <TextBox x:Name="TxtSettingHotkey" Text="Alt+V" IsReadOnly="True"
                                         Style="{StaticResource SettingsTextBoxStyle}"
                                         Foreground="#60A5FA" FontWeight="Bold"/>
                                <Button x:Name="BtnRecordHotkey" Grid.Column="1"
                                        Style="{StaticResource GlassButtonStyle}"
                                        Margin="8,0,0,0" Click="BtnRecordHotkey_Click">
                                    <TextBlock x:Name="TxtRecordHotkeyLabel" Text="Kaydet" FontSize="12"/>
                                </Button>
                            </Grid>

                            <!-- Max items -->
                            <TextBlock x:Name="LblMaxHistoryItems" Text="Gösterilecek Öğe Sayısı"
                                       Foreground="#94A3B8" FontSize="11" FontWeight="SemiBold"
                                       Margin="0,0,0,6"/>
                            <TextBox x:Name="TxtSettingMaxItems" Text="10"
                                     Style="{StaticResource SettingsTextBoxStyle}" Margin="0,0,0,16"/>

                            <!-- File cache limit -->
                            <TextBlock x:Name="LblMaxFileSizeMB" Text="Dosya Önbellekleme Limiti (MB)"
                                       Foreground="#94A3B8" FontSize="11" FontWeight="SemiBold"
                                       Margin="0,0,0,6"/>
                            <TextBox x:Name="TxtSettingMaxSingleFileMB" Text="50"
                                     Style="{StaticResource SettingsTextBoxStyle}" Margin="0,0,0,4"/>
                            <TextBlock x:Name="TxtMaxFileSizeHint"
                                       Text="Bu boyuttan küçük dosyalar arka planda önbelleğe kopyalanır."
                                       FontSize="10" Foreground="#64748B" Margin="0,0,0,16" TextWrapping="Wrap"/>

                            <!-- Checkboxes -->
                            <CheckBox x:Name="ChkAutoCache"
                                      Content="Küçük dosyaları otomatik önbelleğe al"
                                      IsChecked="True" Style="{StaticResource SettingsCheckBoxStyle}"/>
                            <CheckBox x:Name="ChkAutostart"
                                      Content="Windows başlangıcında otomatik çalıştır"
                                      IsChecked="False" Style="{StaticResource SettingsCheckBoxStyle}"/>
                            <!-- Theme Selection -->
                            <TextBlock x:Name="LblTheme" Text="Tema / Theme"
                                       Foreground="{DynamicResource TextSecondaryBrush}" FontSize="11" FontWeight="SemiBold"
                                       Margin="0,0,0,6"/>
                            <ComboBox x:Name="CmbTheme" Style="{StaticResource SettingsComboBoxStyle}" Margin="0,0,0,20">
                                <ComboBoxItem Content="Sistem Varsayılanı" Tag="System" IsSelected="True"/>
                                <ComboBoxItem Content="Koyu Tema" Tag="Dark"/>
                                <ComboBoxItem Content="Açık Tema" Tag="Light"/>
                            </ComboBox>

                            <!-- Save -->
                            <Button x:Name="BtnSaveSettings" Style="{StaticResource GlassButtonStyle}"
                                    Background="#3B82F6" BorderBrush="#60A5FA"
                                    Click="BtnSaveSettings_Click" Padding="12,10">
                                <StackPanel Orientation="Horizontal" HorizontalAlignment="Center">
                                    <TextBlock Text="&#xE74E;" FontFamily="Segoe MDL2 Assets"
                                               FontSize="13" VerticalAlignment="Center" Margin="0,0,7,0"/>
                                    <TextBlock x:Name="TxtSaveSettingsLabel" Text="Ayarları Kaydet"
                                               FontWeight="Bold" FontSize="13"/>
                                </StackPanel>
                            </Button>

                            <TextBlock x:Name="TxtAuthorLicense"
                                       Text="ETDClip v1.0.1   Emir Tuğra Dağ   MIT Lisansı"
                                       FontSize="10" Foreground="#64748B"
                                       HorizontalAlignment="Center" Margin="0,14,0,0"
                                       TextAlignment="Center"/>
                        </StackPanel>
                    </ScrollViewer>
                </Border>
            </Grid>
        </Grid>
    </Border>
</Window>

